############################################################
##              Sketchy Cheat Menu Extended               ##
##                 Bulk Section Helpers                   ##
############################################################
##                                                        ##
## Section-level helpers used by the add_all_X /          ##
## remove_all_X bulk decisions. clear_X_traits_effect     ##
## strips one category; clear_X_mutex_effect adds         ##
## asymmetric engine-mutex extras beyond it.              ##
## add_all_X_effect grants every trait in a category.     ##
##                                                        ##
############################################################

clear_virtue_traits_effect = {
  if = { limit = { trait = chaste }     remove_trait = chaste     }
  if = { limit = { trait = temperate }  remove_trait = temperate  }
  if = { limit = { trait = kind }       remove_trait = kind       }
  if = { limit = { trait = charitable } remove_trait = charitable }
  if = { limit = { trait = patient }    remove_trait = patient    }
  if = { limit = { trait = diligent }   remove_trait = diligent   }
  if = { limit = { trait = humble }     remove_trait = humble     }
}

clear_sin_traits_effect = {
  if = { limit = { trait = lustful }    remove_trait = lustful    }
  if = { limit = { trait = greedy }     remove_trait = greedy     }
  if = { limit = { trait = slothful }   remove_trait = slothful   }
  if = { limit = { trait = gluttonous } remove_trait = gluttonous }
  if = { limit = { trait = wroth }      remove_trait = wroth      }
  if = { limit = { trait = proud }      remove_trait = proud      }
  if = { limit = { trait = envious }    remove_trait = envious    }
}

clear_virtue_mutex_effect = {
  clear_sin_traits_effect = yes
  # kind opposes cruel
  if = { limit = { trait = cruel } remove_trait = cruel }
}

clear_positive_personality_effect = {
  if = { limit = { trait = ambitious }  remove_trait = ambitious  }
  if = { limit = { trait = brave }      remove_trait = brave      }
  remove_physique_trait_effect = yes
  if = { limit = { trait = sturdy }     remove_trait = sturdy     }
  if = { limit = { trait = zealous }    remove_trait = zealous    }
  if = { limit = { trait = erudite }    remove_trait = erudite    }
  if = { limit = { trait = gregarious } remove_trait = gregarious }
  if = { limit = { trait = groomed }    remove_trait = groomed    }
  if = { limit = { trait = honest }     remove_trait = honest     }
  if = { limit = { trait = just }       remove_trait = just       }
  if = { limit = { trait = shrewd }     remove_trait = shrewd     }
  if = { limit = { trait = trusting }   remove_trait = trusting   }
}

clear_negative_personality_effect = {
  if = { limit = { trait = content }   remove_trait = content   }
  if = { limit = { trait = craven }    remove_trait = craven    }
  remove_feeble_trait_effect = yes
  if = { limit = { trait = cynical }   remove_trait = cynical   }
  if = { limit = { trait = shy }       remove_trait = shy       }
  if = { limit = { trait = uncouth }   remove_trait = uncouth   }
  if = { limit = { trait = deceitful } remove_trait = deceitful }
  if = { limit = { trait = arbitrary } remove_trait = arbitrary }
  if = { limit = { trait = dull }      remove_trait = dull      }
  if = { limit = { trait = paranoid }  remove_trait = paranoid  }
  if = { limit = { trait = cruel }     remove_trait = cruel     }
  if = { limit = { trait = stubborn }  remove_trait = stubborn  }
}

clear_childhood_traits_effect = {
  if = { limit = { trait = affectionate }  remove_trait = affectionate  }
  if = { limit = { trait = brooding }      remove_trait = brooding      }
  if = { limit = { trait = conscientious } remove_trait = conscientious }
  if = { limit = { trait = curious }       remove_trait = curious       }
  if = { limit = { trait = fussy }         remove_trait = fussy         }
  if = { limit = { trait = haughty }       remove_trait = haughty       }
  if = { limit = { trait = idolizer }      remove_trait = idolizer      }
  if = { limit = { trait = indolent }      remove_trait = indolent      }
  if = { limit = { trait = playful }       remove_trait = playful       }
  if = { limit = { trait = rowdy }         remove_trait = rowdy         }
  if = { limit = { trait = timid }         remove_trait = timid         }
  if = { limit = { trait = willful }       remove_trait = willful       }
}

clear_positive_personality_mutex_effect = {
  clear_negative_personality_effect = yes
  # zealous strips faith sympathies
  if = { limit = { trait = sympathy_christendom }    remove_trait = sympathy_christendom    }
  if = { limit = { trait = sympathy_islam }          remove_trait = sympathy_islam          }
  if = { limit = { trait = sympathy_pagans }         remove_trait = sympathy_pagans         }
  if = { limit = { trait = sympathy_zoroastrianism } remove_trait = sympathy_zoroastrianism }
  if = { limit = { trait = sympathy_judaism }        remove_trait = sympathy_judaism        }
  if = { limit = { trait = sympathy_indian }         remove_trait = sympathy_indian         }
  # brawny strips sturdy via engine mutex; explicit for tooltip clarity (engine-mutex strips don't render in hover)
  if = { limit = { trait = sturdy } remove_trait = sturdy }
}

clear_negative_personality_mutex_effect = {
  clear_positive_personality_effect = yes
  # cruel opposes kind
  if = { limit = { trait = kind } remove_trait = kind }
}

clear_lifestyle_traits_effect = {
  if = { limit = { trait = administrator } remove_trait = administrator }
  if = { limit = { trait = architect }     remove_trait = architect     }
  if = { limit = { trait = celibate }      remove_trait = celibate      }
  if = { limit = { trait = duelist }       remove_trait = duelist       }
  if = { limit = { trait = falconer }      remove_trait = falconer      }
  if = { limit = { trait = faqih }         remove_trait = faqih         }
  remove_gamer_trait_effect = yes
  if = { limit = { trait = gardener }      remove_trait = gardener      }
  if = { limit = { trait = hedonist }      remove_trait = hedonist      }
  if = { limit = { trait = hunter }        remove_trait = hunter        }
  if = { limit = { trait = impaler }       remove_trait = impaler       }
  if = { limit = { trait = mystic }        remove_trait = mystic        }
  if = { limit = { trait = physician }     remove_trait = physician     }
  if = { limit = { trait = poet }          remove_trait = poet          }
  remove_schemer_trait_effect = yes
  if = { limit = { trait = scholar }       remove_trait = scholar       }
  if = { limit = { trait = socializer }    remove_trait = socializer    }
  if = { limit = { trait = strategist }    remove_trait = strategist    }
  if = { limit = { trait = theologian }    remove_trait = theologian    }
  remove_seducer_strict_trait_effect = yes
  remove_seductress_strict_trait_effect = yes
}

clear_lifestyle_mutex_effect = {
  # hedonist / seducer / seductress all oppose celibate; drop celibate to keep all three
  if = { limit = { trait = celibate } remove_trait = celibate }
}

clear_sympathy_traits_effect = {
  if = { limit = { trait = sympathy_christendom }    remove_trait = sympathy_christendom    }
  if = { limit = { trait = sympathy_indian }         remove_trait = sympathy_indian         }
  if = { limit = { trait = sympathy_islam }          remove_trait = sympathy_islam          }
  if = { limit = { trait = sympathy_judaism }        remove_trait = sympathy_judaism        }
  if = { limit = { trait = sympathy_pagans }         remove_trait = sympathy_pagans         }
  if = { limit = { trait = sympathy_zoroastrianism } remove_trait = sympathy_zoroastrianism }
}

clear_sympathy_mutex_effect = {
  # sympathies oppose zealous
  if = { limit = { trait = zealous } remove_trait = zealous }
}

clear_leadership_traits_effect = {
  if = { limit = { trait = aggressive_leader }     remove_trait = aggressive_leader     }
  if = { limit = { trait = defensive_leader }      remove_trait = defensive_leader      }
  if = { limit = { trait = flanker }               remove_trait = flanker               }
  if = { limit = { trait = holy_warrior }          remove_trait = holy_warrior          }
  if = { limit = { trait = inspiring_leader }      remove_trait = inspiring_leader      }
  if = { limit = { trait = organizer }             remove_trait = organizer             }
  if = { limit = { trait = siege_leader }          remove_trait = siege_leader          }
  if = { limit = { trait = trickster }             remove_trait = trickster             }
  if = { limit = { trait = unyielding_leader }     remove_trait = unyielding_leader     }
  if = { limit = { trait = winter_soldier }        remove_trait = winter_soldier        }
  if = { limit = { trait = cavalry_leader }        remove_trait = cavalry_leader        }
  if = { limit = { trait = heavy_infantry_leader } remove_trait = heavy_infantry_leader }
  if = { limit = { trait = light_foot_leader }     remove_trait = light_foot_leader     }
  if = { limit = { trait = war_elephant_leader }   remove_trait = war_elephant_leader   }
  remove_battlefield_terrain_master_trait_effect = yes
  remove_desert_terrain_trait_effect = yes
  remove_direct_leader_trait_effect = yes
  remove_flat_terrain_trait_effect = yes
  remove_jungle_terrain_trait_effect = yes
  remove_mountain_terrain_trait_effect = yes
  remove_rough_terrain_trait_effect = yes
}

clear_warriors_traits_effect = {
  if = { limit = { trait = adventurer }   remove_trait = adventurer   }
  if = { limit = { trait = berserker }    remove_trait = berserker    }
  if = { limit = { trait = gladiator }    remove_trait = gladiator    }
  if = { limit = { trait = shieldmaiden } remove_trait = shieldmaiden }
  if = { limit = { trait = varangian }    remove_trait = varangian    }
}

clear_chinese_imperial_traits_effect = {
  if = { limit = { trait = logistics_expert } remove_trait = logistics_expert }
  if = { limit = { trait = master_of_flame }  remove_trait = master_of_flame  }
  if = { limit = { trait = sapper }           remove_trait = sapper           }
  if = { limit = { trait = levy_coordinator } remove_trait = levy_coordinator }
  clear_kowtow_traits_effect = yes
}

clear_cheat_traits_effect = {
  if = { limit = { trait = beloved }     remove_trait = beloved     }
  if = { limit = { trait = immortal }    remove_trait = immortal    }
  if = { limit = { trait = builder }     remove_trait = builder     }
  if = { limit = { trait = mastermind }  remove_trait = mastermind  }
  if = { limit = { trait = landlord }    remove_trait = landlord    }
  if = { limit = { trait = conspirator } remove_trait = conspirator }
}

## ===== Add-all bulks ===== ##

add_all_cheat_traits_effect = {
  if = { limit = { NOT = { trait = beloved } }     add_trait = beloved     }
  if = { limit = { NOT = { trait = immortal } }    add_trait = immortal    }
  if = { limit = { NOT = { trait = builder } }     add_trait = builder     }
  if = { limit = { NOT = { trait = mastermind } }  add_trait = mastermind  }
  if = { limit = { NOT = { trait = landlord } }    add_trait = landlord    }
  if = { limit = { NOT = { trait = conspirator } } add_trait = conspirator }
}

add_all_virtues_effect = {
  clear_virtue_mutex_effect = yes
  if = { limit = { NOT = { trait = chaste } }     add_trait = chaste     }
  if = { limit = { NOT = { trait = temperate } }  add_trait = temperate  }
  if = { limit = { NOT = { trait = kind } }       add_trait = kind       }
  if = { limit = { NOT = { trait = charitable } } add_trait = charitable }
  if = { limit = { NOT = { trait = patient } }    add_trait = patient    }
  if = { limit = { NOT = { trait = diligent } }   add_trait = diligent   }
  if = { limit = { NOT = { trait = humble } }     add_trait = humble     }
}

add_all_sins_effect = {
  clear_virtue_traits_effect = yes
  if = { limit = { NOT = { trait = lustful } }    add_trait = lustful    }
  if = { limit = { NOT = { trait = greedy } }     add_trait = greedy     }
  if = { limit = { NOT = { trait = slothful } }   add_trait = slothful   }
  if = { limit = { NOT = { trait = gluttonous } } add_trait = gluttonous }
  if = { limit = { NOT = { trait = wroth } }      add_trait = wroth      }
  if = { limit = { NOT = { trait = proud } }      add_trait = proud      }
  if = { limit = { NOT = { trait = envious } }    add_trait = envious    }
}

add_positive_personality_effect = {
  clear_positive_personality_mutex_effect = yes
  if = { limit = { NOT = { trait = ambitious } }                add_trait = ambitious            }
  if = { limit = { NOT = { trait = brave } }                    add_trait = brave                }
  if = { limit = { NOT = { has_physique_trait_trigger = yes } } add_physique_trait_effect = yes  }
  if = { limit = { NOT = { trait = zealous } }                  add_trait = zealous              }
  if = { limit = { NOT = { trait = erudite } }                  add_trait = erudite              }
  if = { limit = { NOT = { trait = gregarious } }               add_trait = gregarious           }
  if = { limit = { NOT = { trait = groomed } }                  add_trait = groomed              }
  if = { limit = { NOT = { trait = honest } }                   add_trait = honest               }
  if = { limit = { NOT = { trait = just } }                     add_trait = just                 }
  if = { limit = { NOT = { trait = shrewd } }                   add_trait = shrewd               }
  if = { limit = { NOT = { trait = trusting } }                 add_trait = trusting             }
}

add_negative_personality_effect = {
  clear_negative_personality_mutex_effect = yes
  if = { limit = { NOT = { trait = content } }                add_trait = content            }
  if = { limit = { NOT = { trait = craven } }                 add_trait = craven             }
  if = { limit = { NOT = { has_feeble_trait_trigger = yes } } add_feeble_trait_effect = yes  }
  if = { limit = { NOT = { trait = cynical } }                add_trait = cynical            }
  if = { limit = { NOT = { trait = shy } }                    add_trait = shy                }
  if = { limit = { NOT = { trait = uncouth } }                add_trait = uncouth            }
  if = { limit = { NOT = { trait = deceitful } }              add_trait = deceitful          }
  if = { limit = { NOT = { trait = arbitrary } }              add_trait = arbitrary          }
  if = { limit = { NOT = { trait = dull } }                   add_trait = dull               }
  if = { limit = { NOT = { trait = paranoid } }               add_trait = paranoid           }
  if = { limit = { NOT = { trait = cruel } }                  add_trait = cruel              }
  if = { limit = { NOT = { trait = stubborn } }               add_trait = stubborn           }
}

add_all_leadership_effect = {
  if = { limit = { NOT = { trait = aggressive_leader } }                          add_trait = aggressive_leader                     }
  if = { limit = { NOT = { has_battlefield_terrain_master_trait_trigger = yes } } add_battlefield_terrain_master_trait_effect = yes }
  if = { limit = { NOT = { trait = defensive_leader } }                           add_trait = defensive_leader                      }
  if = { limit = { NOT = { has_direct_leader_trait_trigger = yes } }              add_direct_leader_trait_effect = yes              }
  if = { limit = { NOT = { trait = flanker } }                                    add_trait = flanker                               }
  if = { limit = { NOT = { trait = holy_warrior } }                               add_trait = holy_warrior                          }
  if = { limit = { NOT = { trait = inspiring_leader } }                           add_trait = inspiring_leader                      }
  if = { limit = { NOT = { trait = organizer } }                                  add_trait = organizer                             }
  if = { limit = { NOT = { trait = siege_leader } }                               add_trait = siege_leader                          }
  if = { limit = { NOT = { trait = trickster } }                                  add_trait = trickster                             }
  if = { limit = { NOT = { trait = unyielding_leader } }                          add_trait = unyielding_leader                     }
  if = { limit = { NOT = { trait = winter_soldier } }                             add_trait = winter_soldier                        }
  if = { limit = { NOT = { trait = cavalry_leader } }                             add_trait = cavalry_leader                        }
  if = { limit = { NOT = { trait = heavy_infantry_leader } }                      add_trait = heavy_infantry_leader                 }
  if = { limit = { NOT = { trait = light_foot_leader } }                          add_trait = light_foot_leader                     }
  if = { limit = { NOT = { trait = war_elephant_leader } }                        add_trait = war_elephant_leader                   }
  if = { limit = { NOT = { has_desert_terrain_trait_trigger = yes } }             add_desert_terrain_trait_effect = yes             }
  if = { limit = { NOT = { has_flat_terrain_trait_trigger = yes } }               add_flat_terrain_trait_effect = yes               }
  if = { limit = { NOT = { has_jungle_terrain_trait_trigger = yes } }             add_jungle_terrain_trait_effect = yes             }
  if = { limit = { NOT = { has_mountain_terrain_trait_trigger = yes } }           add_mountain_terrain_trait_effect = yes           }
  if = { limit = { NOT = { has_rough_terrain_trait_trigger = yes } }              add_rough_terrain_trait_effect = yes              }
}

add_all_warriors_effect = {
  if = { limit = { NOT = { trait = adventurer } }                   add_trait = adventurer   }
  if = { limit = { NOT = { trait = berserker } }                    add_trait = berserker    }
  if = { limit = { NOT = { trait = gladiator } }                    add_trait = gladiator    }
  if = { limit = { is_female = yes NOT = { trait = shieldmaiden } } add_trait = shieldmaiden }
  if = { limit = { NOT = { trait = varangian } }                    add_trait = varangian    }
}

add_all_chinese_imperial_effect = {
  if = { limit = { NOT = { trait = logistics_expert } }         add_trait = logistics_expert            }
  if = { limit = { NOT = { trait = master_of_flame } }          add_trait = master_of_flame             }
  if = { limit = { NOT = { trait = sapper } }                   add_trait = sapper                      }
  if = { limit = { NOT = { trait = levy_coordinator } }         add_trait = levy_coordinator            }
  if = { limit = { trait = kow_tow_completed_tier_1 }           remove_trait = kow_tow_completed_tier_1 }
  if = { limit = { trait = kow_tow_completed_tier_2 }           remove_trait = kow_tow_completed_tier_2 }
  if = { limit = { NOT = { trait = kow_tow_completed_tier_3 } } add_trait = kow_tow_completed_tier_3    }
}

add_all_chinese_imperial_vanilla_effect = {
  if = { limit = { NOT = { trait = logistics_expert } } add_trait = logistics_expert }
  if = { limit = { NOT = { trait = master_of_flame } }  add_trait = master_of_flame  }
  if = { limit = { NOT = { trait = sapper } }           add_trait = sapper           }
  if = { limit = { NOT = { trait = levy_coordinator } } add_trait = levy_coordinator }
}

add_all_lifestyles_effect = {
  clear_lifestyle_mutex_effect = yes
  if = { limit = { NOT = { trait = administrator } }           add_trait = administrator      }
  if = { limit = { NOT = { trait = architect } }               add_trait = architect          }
  if = { limit = { NOT = { trait = duelist } }                 add_trait = duelist            }
  if = { limit = { NOT = { trait = falconer } }                add_trait = falconer           }
  if = { limit = { NOT = { has_gamer_trait_trigger = yes } }   add_gamer_trait_effect = yes   }
  if = { limit = { NOT = { trait = gardener } }                add_trait = gardener           }
  if = { limit = { NOT = { trait = hedonist } }                add_trait = hedonist           }
  if = { limit = { NOT = { trait = hunter } }                  add_trait = hunter             }
  if = { limit = { NOT = { trait = impaler } }                 add_trait = impaler            }
  if = { limit = { NOT = { trait = mystic } }                  add_trait = mystic             }
  if = { limit = { NOT = { trait = physician } }               add_trait = physician          }
  if = { limit = { NOT = { trait = poet } }                    add_trait = poet               }
  if = { limit = { NOT = { has_schemer_trait_trigger = yes } } add_schemer_trait_effect = yes }
  if = { limit = { NOT = { trait = socializer } }              add_trait = socializer         }
  if = { limit = { NOT = { trait = strategist } }              add_trait = strategist         }
  if = { limit = { NOT = { trait = theologian } }              add_trait = theologian         }
  if = {
    limit = {
      religion_group = muslim
      NOT = { trait = faqih }
    }
    if = { limit = { trait = scholar } remove_trait = scholar }
    add_trait = faqih
  }
  if = {
    limit = {
      NOT = { religion_group = muslim }
      NOT = { trait = scholar }
    }
    if = { limit = { trait = faqih } remove_trait = faqih }
    add_trait = scholar
  }
  if = {
    limit = { NOT = { has_seducer_trait_trigger = yes } }
    remove_seducer_strict_trait_effect = yes
    remove_seductress_strict_trait_effect = yes
    add_seducer_trait_effect = yes
  }
}

add_all_sympathies_effect = {
  clear_sympathy_mutex_effect = yes
  if = { limit = { NOT = { trait = sympathy_christendom }    NOT = { religion_group = christian } }         add_trait = sympathy_christendom    }
  if = { limit = { NOT = { trait = sympathy_indian }         NOT = { religion_group = indian_group } }      add_trait = sympathy_indian         }
  if = { limit = { NOT = { trait = sympathy_islam }          NOT = { religion_group = muslim } }            add_trait = sympathy_islam          }
  if = { limit = { NOT = { trait = sympathy_judaism }        NOT = { religion_group = jewish_group } }      add_trait = sympathy_judaism        }
  if = { limit = { NOT = { trait = sympathy_pagans }         NOT = { religion_group = pagan_group } }       add_trait = sympathy_pagans         }
  if = { limit = { NOT = { trait = sympathy_zoroastrianism } NOT = { religion_group = zoroastrian_group } } add_trait = sympathy_zoroastrianism }
}

add_all_childhood_effect = {
  if = { limit = { NOT = { trait = affectionate } }  add_trait = affectionate  }
  if = { limit = { NOT = { trait = brooding } }      add_trait = brooding      }
  if = { limit = { NOT = { trait = conscientious } } add_trait = conscientious }
  if = { limit = { NOT = { trait = curious } }       add_trait = curious       }
  if = { limit = { NOT = { trait = fussy } }         add_trait = fussy         }
  if = { limit = { NOT = { trait = haughty } }       add_trait = haughty       }
  if = { limit = { NOT = { trait = idolizer } }      add_trait = idolizer      }
  if = { limit = { NOT = { trait = indolent } }      add_trait = indolent      }
  if = { limit = { NOT = { trait = playful } }       add_trait = playful       }
  if = { limit = { NOT = { trait = willful } }       add_trait = willful       }
}

